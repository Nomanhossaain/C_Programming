#include<stdio.h>
int main()
{
    int first, middle, last;
    printf("Enter the first number: ");
    scanf("%d",&first);

    printf("Enter the middle number: ");
    scanf("%d",&middle);

    printf("Enter the last number: ");
    scanf("%d",&last);

    printf("The first value: %d\nThe last value: %d", first, last);
}